const config = {
    GITHUB_TOKEN: process.env.GITHUB_TOKEN || '',
    REPO_OWNER: process.env.vilasp29 || '',
    REPO_NAME: process.env.SPDP25 || '',
    FILE_PATH: process.env.FILE_PATH || 'rsvps.json'
};

export default config;
